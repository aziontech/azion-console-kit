{
  "$comment": "Human context per cell (resource:journey). Informational only — merged into the generated coverage-matrix.json. Coverage truth lives in @covers markers inside the test files.",
  "notes": {
    "application:J1": "createDraft mutation + invalidation (shared service contract) and now dedicated edge-app tests: createDraft POST with the mapped payload, clone maps sourceVersionId/comment, bare clone omits resource fields (empty-vs-clone closed in Wave 1). Gap: \"new draft opens in the editor\" navigation (functional/e2e).",
    "application:J10": "command-error emitted (never rejects), backend rejection → error toast, failed build does not call onSuccess, no success signal on rejection. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "application:J2": "Application SAVE updates the draft (explicit case); dedicated edge-app tests assert updateDraft PATCH with the root-modules payload and the adapter form→payload mapping; updateDraft/patchDraft invalidation. Wave 1 closed the shared gaps: invalid SAVE rejects and never touches the service (req 2.5, form-adapter lifecycle) and a disabled Save renders disabled and dispatches nothing (req 3.1, functional action bar). PBT hardens transformDraftPayload (stripUndefined at every depth).",
    "application:J3": "SAVE_AND_BUILD order save→build asserted (explicit Application case) + dedicated edge-app build endpoint test; kebab BUILD hits /build directly. Wave 1 added the generic save→build ordering (form-adapter lifecycle) and post-command navigation (edit screen returns to the versions list with a success toast). Remaining gap: rendered building/queued state badge in the listing.",
    "application:J4": "CANCEL_BUILD routed to service + cancelBuild invalidation + dedicated edge-app cancelBuild endpoint test. Wave 1: ProcessingOverlay rendered while building, hidden when ready, Cancel emits cancel (functional dialogs suite).",
    "application:J5": "NEW_DRAFT_FROM handler routes to service; kebab menu row present per state. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "application:J6": "Comment contract, archive invalidation + base guard (empty comment rejects), 'in use' backend rejection surfaces a toast with no success/reload — all asserted at component level (shared shell path). Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "application:J7": "Destructive confirm dialog (deletes only after confirmation), deleteVersion invalidation, rejection keeps list intact (no success/reload) — component level. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "application:J8": "DEPLOY registered by capability, heading/footer buttons rendered, kebab DEPLOY routes to the composer scoped to the version, PROMOTE opens the drawer pinned to the version. Tests are resource-generic over the shared (no-fork) shell. Wave 1 functional: heading Deploy renders and routes to the composer (explicit + resource-scoped route); action bar Deploy/Redeploy per state; deploy-resource-context builds the drawer context and version options.",
    "application:J9": "Structural governance (every versioned form consumes the central readOnly) + state machine gating + Wave 1 rendered proof: a control consuming the version context renders disabled and a real click cannot toggle it (functional readonly-context). Per-form rendered pass for the application form stays e2e-deferred (ADR 7.4).",
    "connector:J1": "createDraft mutation + invalidation + adapter payload (connector is NOT in the shared invalidation suite; its own service test carries the contract). Gap: open-in-editor navigation.",
    "connector:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "connector:J2": "updateDraft/patchDraft + adapter mapping at service level. Wave 1 proved the shared shell SAVE flow generically (valid SAVE persists, invalid SAVE rejects — form-adapter lifecycle) and the rendered disabled Save (functional action bar).",
    "connector:J3": "build POSTs /versions/{vid}/build and invalidates; kebab BUILD runs the endpoint directly. Wave 1 added the generic save→build ordering (form-adapter lifecycle) and post-command navigation (edit screen returns to the versions list with a success toast). Remaining gap: rendered building/queued state badge in the listing.",
    "connector:J4": "cancelBuild POSTs /versions/{vid}/cancel and invalidates. Wave 1: overlay rendered behavior closed generically (functional dialogs suite).",
    "connector:J5": "NEW_DRAFT_FROM exercised in the connector v6 view test + clone payload in the adapter. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "connector:J6": "Shared archive contract + connector archive (non-empty comment required) service test. Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "connector:J7": "Shared delete confirm + connector deleteVersion service test. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "connector:J8": "Deployable affordances via the shared (no-fork) shell — resource-generic tests. Wave 1 functional: heading Deploy renders and routes to the composer (explicit + resource-scoped route); action bar Deploy/Redeploy per state; deploy-resource-context builds the drawer context and version options.",
    "connector:J9": "Rendered read-only asserted: mounts with VERSION_CONTEXT readOnly true/false and reacts to the toggle.",
    "custom_page:J1": "createDraft mutation + invalidation + adapter payload. Gap: open-in-editor navigation.",
    "custom_page:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "custom_page:J2": "saveStrategy (req 6.5) explicitly asserted: SAVE hits the BASE endpoint, NOT version updateDraft. Wave 1 closed the shared gaps: invalid-SAVE gate and rendered disabled Save (form-adapter lifecycle + functional action bar).",
    "custom_page:J3": "build endpoint + invalidation covered at service level; post-command navigation now generic via the edit screen. Gaps: no explicit custom_page base-save→build sequence test; rendered state transition in the listing.",
    "custom_page:J4": "cancelBuild service contract. Wave 1: overlay rendered behavior (shows while building, hides when ready, Cancel emits) closed generically (functional dialogs suite).",
    "custom_page:J5": "Shared handler + clone payload. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "custom_page:J6": "Shared archive contract + custom-page archive service test. Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "custom_page:J7": "Shared delete confirm + custom-page deleteVersion service test. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "custom_page:J8": "Deployable affordances via the shared (no-fork) shell — resource-generic tests. Wave 1 functional: heading Deploy renders and routes to the composer (explicit + resource-scoped route); action bar Deploy/Redeploy per state; deploy-resource-context builds the drawer context and version options.",
    "custom_page:J9": "Structural governance + Wave 1 rendered proof that consuming the version context disables the control (functional readonly-context). Per-form rendered pass for the custom-page form stays e2e-deferred (ADR 7.4).",
    "deployment:J1": "createVersionService POSTs /versions and invalidates BOTH the version cache and deployments.detail (special hook tested); Wave 1 closed the clone payload gap (transformCreateDraftPayload keeps only resource {id, resource_type} + strategy/origin). Gap: open-in-editor navigation.",
    "deployment:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure. Deployment-api specific failure handling still untested.",
    "deployment:J2": "Closed in Wave 1: updateDraft PATCHes /versions/{vid} under deployment-api and invalidates both the version cache and deployments.detail (deployment-version-service); draft payload mapping asserted (deployment-version-adapter); shared invalid-SAVE gate + rendered disabled Save proven generically.",
    "deployment:J3": "Closed the service gap in Wave 1: build POSTs /versions/{vid}/build under deployment-api and invalidates both caches (deployment-version-service). Generic save→build ordering and post-command navigation also proven. Remaining gap: rendered building/queued badge in the listing; the release-composer build&activate path is the composer contract (J8), not this journey.",
    "deployment:J4": "Closed in Wave 1: cancelBuild POSTs /versions/{vid}/cancel under deployment-api and invalidates both caches (deployment-version-service); overlay rendered behavior proven generically (functional dialogs suite).",
    "deployment:J5": "Deployment's analogue is revert: POST /versions/{vid}/revert generates a NEW version and invalidates both caches; the Version History tab wires the revert action. Wave 1 closed the generic NEW_DRAFT_FROM navigation (edit screen). Remaining gaps: kebab NEW_DRAFT_FROM path for deployment specifically; editor remount via :key.",
    "deployment:J6": "Shared (service-agnostic) archive contract + Wave 1 archive payload mapping (transformArchivePayload carries only reason + comment) and rendered dialog behavior (generic). Still no archive call exercised against deployment-api.",
    "deployment:J7": "Shared delete confirm + Wave 1 deployment-specific test: deleteVersion DELETEs /versions/{vid} under deployment-api and invalidates both caches; destructive confirm rendered generically (functional dialogs suite).",
    "deployment:J8": "Special case: deployment IS the deploy target — the release composer surface and build&activate payload are tested. Gap: the version-shell Deploy hand-off (heading/kebab → composer) is only covered by the resource-generic shared tests, not a deployment-specific case. Wave 1 adds the generic rendered heading/action-bar deploy affordances and the deploy-resource-context contract.",
    "deployment:J9": "Structural scan: Version History performs no version mutation except revert; deployment form fields stay outside the version context. Gap: rendered disabled-fields assertion for an immutable deployment version.",
    "firewall:J1": "createDraft service + invalidation + adapter payload. Gap: open-in-editor navigation.",
    "firewall:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "firewall:J2": "Save via shared handler + adapter mapping; composite sub-resource CRUD scoped by (resourceId, versionId) tested (req 6.4). Wave 1 closed the shared gaps: invalid SAVE rejects and never touches the service, valid SAVE persists the merged draft (lifecycle) and rendered disabled Save (functional action bar).",
    "firewall:J3": "Explicit Firewall SAVE_AND_BUILD (updates then builds, default strategy) + build service test. Wave 1 added the generic save→build ordering (form-adapter lifecycle) and post-command navigation (edit screen returns to the versions list with a success toast). Remaining gap: rendered building/queued state badge in the listing.",
    "firewall:J4": "cancelBuild service contract. Wave 1: overlay rendered behavior (shows while building, hides when ready, Cancel emits) closed generically (functional dialogs suite).",
    "firewall:J5": "Shared handler + clone payload. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "firewall:J6": "Shared archive contract + firewall archive service test. Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "firewall:J7": "Shared delete confirm + firewall deleteVersion service test. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "firewall:J8": "Deployable affordances via the shared (no-fork) shell — resource-generic tests. Wave 1 functional: heading Deploy renders and routes to the composer (explicit + resource-scoped route); action bar Deploy/Redeploy per state; deploy-resource-context builds the drawer context and version options.",
    "firewall:J9": "Structural governance + version editor tabs + Wave 1 rendered proof that consuming the version context disables the control (functional readonly-context). Per-form rendered pass for the firewall form stays e2e-deferred (ADR 7.4).",
    "function:J1": "createDraft (clone maps resource fields to root snake_case) + invalidation + view-level adapter usage. Gap: open-in-editor navigation.",
    "function:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "function:J2": "updateDraft PATCH + adapter mapping at service level. Wave 1 proved the shared shell SAVE flow generically (valid SAVE persists, invalid SAVE rejects — form-adapter lifecycle) and the rendered disabled Save (functional action bar).",
    "function:J3": "build endpoint + invalidation; kebab BUILD direct. Wave 1 added the generic save→build ordering (form-adapter lifecycle) and post-command navigation (edit screen returns to the versions list with a success toast). Remaining gap: rendered building/queued state badge in the listing.",
    "function:J4": "cancelBuild endpoint + invalidation; CANCEL_BUILD referenced at view level. Wave 1: overlay rendered behavior closed generically (functional dialogs suite).",
    "function:J5": "NEW_DRAFT_FROM exercised at view level; versioned-only kebab keeps the action. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "function:J6": "Shared archive contract + function archive (comment required, POST with comment) service tests. Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "function:J7": "Shared delete confirm + function deleteVersion service test. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "function:J8": "versioned-only: journey does not apply. The NEGATIVE affordance is what must hold (req 6.3) and is covered: no DEPLOY/PROMOTE/ROLLBACK in any state, heading/footer/kebab hide Deploy, dispatch('DEPLOY') fail-closes, landing omits the promote seam. Wave 1: heading omits Deploy (rendered), action bar drops the Deploy button AND the deploy copy, deploy-resource-context yields null, and the PBT proves no DEPLOY/PROMOTE/ROLLBACK in any state.",
    "function:J9": "Rendered read-only asserted: code editor becomes read-only when the version context is immutable and reacts to the toggle.",
    "network_list:J1": "createDraft + invalidation + view-level adapter usage. Gap: open-in-editor navigation.",
    "network_list:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "network_list:J2": "updateDraft + adapter mapping at service level. Wave 1 proved the shared shell SAVE flow generically (valid SAVE persists, invalid SAVE rejects — form-adapter lifecycle) and the rendered disabled Save (functional action bar).",
    "network_list:J3": "build endpoint + invalidation; kebab BUILD direct. Wave 1 added the generic save→build ordering (form-adapter lifecycle) and post-command navigation (edit screen returns to the versions list with a success toast). Remaining gap: rendered building/queued state badge in the listing.",
    "network_list:J4": "cancelBuild endpoint + invalidation. Wave 1: overlay rendered behavior closed generically (functional dialogs suite).",
    "network_list:J5": "NEW_DRAFT_FROM exercised at view level; versioned-only kebab keeps the action. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "network_list:J6": "Shared archive contract + network-list archive service test (in the shared invalidation suite too). Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "network_list:J7": "Shared delete confirm + network-list deleteVersion service test. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "network_list:J8": "versioned-only: n/a; negative affordance (req 6.3) covered — no Deploy/Promote/Rollback in any state or surface. Wave 1: heading omits Deploy (rendered), action bar drops the Deploy button AND the deploy copy, deploy-resource-context yields null, and the PBT proves no DEPLOY/PROMOTE/ROLLBACK in any state.",
    "network_list:J9": "Rendered read-only asserted per field (asn, ip/cidr list, countries) including reactive toggle to immutable.",
    "waf:J1": "createDraft + invalidation + adapter payload. Gap: open-in-editor navigation.",
    "waf:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "waf:J2": "updateDraft + adapter mapping; composite versioned exceptions CRUD scoped by (resourceId, versionId) tested and invalidating the version cache (req 6.4). Wave 1 proved the shared shell SAVE flow generically (invalid-SAVE gate) and the rendered disabled Save (functional action bar).",
    "waf:J3": "build endpoint + invalidation; kebab BUILD direct. Wave 1 added the generic save→build ordering (form-adapter lifecycle) and post-command navigation (edit screen returns to the versions list with a success toast). Remaining gap: rendered building/queued state badge in the listing.",
    "waf:J4": "cancelBuild endpoint + invalidation. Wave 1: overlay rendered behavior closed generically (functional dialogs suite).",
    "waf:J5": "Clone payload + versioned-only kebab keeps NEW_DRAFT_FROM. Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "waf:J6": "Shared archive contract + WAF archive service test. Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "waf:J7": "Shared delete confirm + WAF deleteVersion service test. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "waf:J8": "versioned-only: n/a; negative affordance (req 6.3) covered — no Deploy/Promote/Rollback in any state or surface. Wave 1: heading omits Deploy (rendered), action bar drops the Deploy button AND the deploy copy, deploy-resource-context yields null, and the PBT proves no DEPLOY/PROMOTE/ROLLBACK in any state.",
    "waf:J9": "Rendered read-only asserted: write actions removed in an immutable version, create drawer does not open when read-only.",
    "workload:J1": "createDraft invalidation + transformCreateDraftPayload (clone carries source_version + comment; empty body → empty payload). Gap: 'new draft opens in the editor' navigation.",
    "workload:J10": "Shared error path only. Wave 1: edit-screen command-error routing asserted (showErrors delegate, generic error toast, SAVE success reloads in place). Remaining gaps: rendered error toast per command surface; explicit no-navigation-on-failure.",
    "workload:J2": "Workload SAVE PUTs the draft (explicit case); draft payload mapping + stripUndefined asserted. Wave 1 closed the shared gaps: invalid-SAVE gate (rejects, never touches the service) and rendered disabled Save (functional action bar).",
    "workload:J3": "Workload saveStrategy (req 6.5) explicitly asserted: SAVE and SAVE_AND_BUILD both PUT with NO separate build; kebab Build navigates with ?intent=build (AUTO_BUILD_ON_SAVE). Wave 1 added post-command navigation generically (edit screen). Remaining gap: rendered state transition in the listing.",
    "workload:J4": "cancelBuild invalidation via the shared base contract. Wave 1: overlay rendered behavior (shows while building, hides when ready, Cancel emits) closed generically (functional dialogs suite).",
    "workload:J5": "Shared NEW_DRAFT_FROM handler + clone payload (source_version + comment, with/without overrides). Wave 1 closed navigate-to-new-draft (edit screen routes to the newly created version). Remaining gap: editor remount via :key.",
    "workload:J6": "Shared archive contract (comment guard, invalidation, in-use rejection) + workload archive payload (comment only). Wave 1: rendered dialog (confirm disabled until a comment is typed) in the functional dialogs suite.",
    "workload:J7": "Shared delete confirm + deleteVersion invalidation. Wave 1: destructive confirm rendered (DELETE confirms without a comment) in the functional dialogs suite.",
    "workload:J8": "Deployable affordances (shared shell) + PROMOTE drawer receives workloadId; regression: workload rollback still invalidates the version cache despite bypassing the hook. Wave 1 functional: heading Deploy renders and routes to the composer (explicit + resource-scoped route); action bar Deploy/Redeploy per state; deploy-resource-context builds the drawer context and version options.",
    "workload:J9": "Structural governance + Wave 1 rendered proof that consuming the version context disables the control (functional readonly-context). Per-form rendered pass for the workload form stays e2e-deferred (ADR 7.4)."
  }
}
