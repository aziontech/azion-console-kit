export default `const html = \`<h1>Hello World</h1>
<p> This markup was generated by Azion - Edge Functions.</p>\`

async function handleRequest(request) {
  return new Response(html, {
    headers: {
      "content-type": "text/html;charset=UTF-8",
    },
  })
}

function PreviewProvider(args) {
  var request = new Request("https://azion.com", {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      "Authorization": "Bearer my-token"
    },
    redirect: "follow",
  });

  return handleRequest(request);
}

// For Edge Firewall functions, use 'firewall' instead of 'fetch' in the addEventListener.

addEventListener("fetch", event => {
  return event.respondWith(handleRequest(event.args))
});
`
